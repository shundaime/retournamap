{{ form_start(form, {
    attr:{
        class: 'form_container'
    }
}) }}
<ul class="row">
    <li class="col-lg-12">{{ form_row(form.content) }}</li>
</ul>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
